%h1
  Messages with #{@customer.name}
#messages
  = render @messages
= form_with model: [@user, @customer, Message.new], local: false do |f|
  %div
    = f.label :content, "New Message"
    %br/
    = f.text_area :content, rows: 4, required: true
  = f.submit "Send Message"