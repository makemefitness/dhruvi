.inner-container-3.mt-3
  %table.table.table-striped
    %thead.thead-dark
      %tr
        %th Name
        %th Additional information
        %th{colspan: "3"}

    %tbody
      - resources.each do |resource|
        %tr
          %td= resource.name.capitalize!
          %td
          /%td= link_to 'Szegoły', resource
          %td
            %button.btn.btn-success{"data-target" => "#myModal-exercise#{resource.id}", "data-toggle" => "modal", :type => "button"}
              Details...
          -# %td= link_to 'Edyt', edit_resource_path(resource), remote: true, class: 'btn my-btn--pink'
          -# %td= link_to 'Delete', resource, method: :delete, data: { confirm: 'Are you sure?' }, remote: true, class: 'btn btn-danger'
  %br